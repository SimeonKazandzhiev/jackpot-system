package com.example.jackpot.util;

import java.math.BigDecimal;

public class BigDecimalRandomGenerator {
    public static BigDecimal generateRandom() {
        return BigDecimal.valueOf(Math.random());
    }
}
